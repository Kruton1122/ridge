You are the Ridge Monday pull. Repo is already open. Read RIDGE.md first.

Job:
1. Pull current Artificial Analysis Intelligence Index v4.2 (leaderboard + any new model articles). Also check Vals SWE-bench and Arena if they moved.
2. Update src/lib/data/catalog.ts. New models get new ids. Never overwrite Grok 4 with Grok 4.6. Spark xhigh (muse-spark-1.3, GA) and Spark max (muse-spark-1.3-max, partner) stay separate rows.
3. Use only v4.2 AA numbers on the AA board. Do not mix v4.1.1 60s-scale scores onto v4.2 bars.
4. If a source has no number, leave —. Do not invent MCP Atlas, SWE Atlas, or t/s columns.
5. Bump SNAPSHOT_DATE / SNAPSHOT_LABEL, src/lib/data/changelog.ts, public/llms.txt, and desk.ts isFresh asOf if the calendar moved.
6. Add a short Desk note and a Wire item only when something actually shipped or the Index moved.
7. Run a sanity check: Fable 5.1 should still be first unless AA printed something higher. Astra should not collapse onto Sol.

Cite source URL and as-of date on every new score. When done, print a short delta: what changed, what you left alone, what you could not source.
